# 本机依赖、构建产物与运行态；源码、打包入口和文档仍由 Git 跟踪。
node_modules/
dist/
web/dist/

# bootstrap / PM Worker 运行时可能包含 Token、绝对路径、日志和任务证据。
.biao/
.codex/
work/
/data/
/dump.rdb

coverage/
.DS_Store
.env
.env.*
!.env.example
*.tgz
*.log
scripts/agent-exec.sh

# 本机 dogfooding 计划目录：plans 属于各部署的工作区数据，不入产品仓库
/plans/

# PM Worker internal runtime state (not source code)
.zcode-pm/
